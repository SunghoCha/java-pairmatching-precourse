package pairmatching.domain;

import org.junit.jupiter.api.Test;

import static org.assertj.core.api.AssertionsForClassTypes.assertThat;
import static org.junit.jupiter.api.Assertions.*;

class MatchResultTest {

//    @Test
//    void getMission() {
//        MatchResult matchResult = new MatchResult();
//        Mission mission = matchResult.getMission("백엔드", "자동차경주");
//        MissionTemplate missionTemplate = mission.getMissionTemplate();
//        assertThat(missionTemplate.getCourse()).isEqualTo(Course.BACKEND);
//        assertThat(missionTemplate.getLevel()).isEqualTo(Level.LEVEL1);
//        assertThat(missionTemplate.getName()).isEqualTo("자동차경주");
//    }
}